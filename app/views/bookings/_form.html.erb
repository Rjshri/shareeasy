<%= form_with(model: booking, html: { id: 'bookingForm' }) do |form| %>

   <%= form.hidden_field :user_id, value: @user.id %>
   <%= form.hidden_field :product_id, value: @product.id %>
   <%# <%= form.hidden_field :product_id, value: @product.id%>
   <%# <%= @product.id%>

  <div>
    <%= form.submit "Confirm Buy" %>
  </div>
<% end %>
